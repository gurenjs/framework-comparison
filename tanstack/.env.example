# Secret used to encrypt the session cookie (32+ characters).
# Required in production; a development-only fallback is used otherwise.
SESSION_SECRET=

# SQLite database file (default: minilog.db in the project directory).
DATABASE_PATH=minilog.db
